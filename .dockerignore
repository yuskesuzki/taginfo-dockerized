# ビルド成果物(ビルド段で作り直すため不要)
build/

# VCS メタデータ(version.cpp の git hash 埋め込みは無くても動く)
.git/

# 実行時の入出力データ(ボリュームにマウントするものでイメージには不要)
# 注意: これらが残っているとビルドコンテキストが肥大化し COPY で失敗する
data/
download/
*.osm.pbf
*.osm.bz2
*.opl
*.db

# macOS
.DS_Store

# 注意: abseil-cpp サブモジュールはビルドに必要なので除外しないこと
